# Supabase Configuration
# Get these values from your Supabase project dashboard (Settings → API)

# Supabase Project URL (e.g., https://xxxxxxxxxxxxx.supabase.co)
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Service Role Key (Settings → API → service_role key)
# ⚠️ KEEP THIS SECRET - Never commit this to git!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database Connection String (Settings → Database → Connection string)
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?sslmode=require
DATABASE_URL=postgresql://postgres:YOUR_DB_PASSWORD@db.your-project-ref.supabase.co:5432/postgres?sslmode=require

# Supabase Project Reference ID (Settings → General)
# Used for CLI linking: supabase link --project-ref <ref>
SUPABASE_PROJECT_REF=your-project-ref

# Optional: Supabase Personal Access Token (for CLI authentication)
# Generate at: https://supabase.com/dashboard/account/tokens
# Used for: supabase login --token <token>
SUPABASE_ACCESS_TOKEN=your-personal-access-token

# S3 Configuration (Hetzner)
S3_ENDPOINT=https://your-endpoint.hetzner.com
S3_ACCESS_KEY_ID=your-access-key
S3_SECRET_ACCESS_KEY=your-secret-key
S3_BUCKET_NAME=cortana-vision-prod
S3_REGION=eu-central

# Application Configuration
ENVIRONMENT=development
LOG_LEVEL=info
